import { FormatLocalDateTimePipe } from './format-local-date-time.pipe';

describe('FormatLocalDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLocalDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
