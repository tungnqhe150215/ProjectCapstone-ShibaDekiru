import { SubmitFileExercise} from './submit-file-exercise';

describe('MultipleChoiceExercise', () => {
  it('should create an instance', () => {
    expect(new SubmitFileExercise()).toBeTruthy();
  });
});
