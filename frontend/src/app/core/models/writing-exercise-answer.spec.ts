import { WritingExerciseAnswer } from './writing-exercise-answer';

describe('WritingExerciseAnswer', () => {
  it('should create an instance', () => {
    expect(new WritingExerciseAnswer()).toBeTruthy();
  });
});
